# Historic Changelog

> This file preserves the changelog history from the standalone
> [`SAP/guided-development`](https://github.com/SAP/guided-development) repository,
> recorded before the extension was migrated into the `app-studio-toolkit` monorepo.
> New entries are tracked via Changesets in `CHANGELOG.md`.
